{
  "name": "visual-system-react-pdf",
  "version": "1.0.0",
  "description": "Publication-grade PDF generation for medical articles",
  "type": "commonjs",
  "scripts": {
    "build": "babel templates --out-dir dist/templates && cp renderer.js dist/",
    "render": "node dist/renderer.js",
    "test": "npm run build && node dist/renderer.js --template newsletter --output test-output/test.pdf",
    "list": "npm run build && node dist/renderer.js --list",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@react-pdf/renderer": "^3.4.0",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@babel/cli": "^8.0.4",
    "@babel/core": "^8.0.1",
    "@babel/preset-env": "^8.0.2",
    "@babel/preset-react": "^8.0.1"
  }
}
